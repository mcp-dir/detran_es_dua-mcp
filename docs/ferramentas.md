# Ferramentas

DETRAN ES: Emissão de Documento Único de Arrecadação (DUA) expõe 1 ferramenta (todas somente leitura).

### 1. `detran_es_dua_consultar`
**Input**: `placa`, `renavam`, `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

DETRAN ES: Emissão de Documento Único de Arrecadação (DUA), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DETRAN ES: Emissão de Documento Único de Arrecadação (DUA)
```
